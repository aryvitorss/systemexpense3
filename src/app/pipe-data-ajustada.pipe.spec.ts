import { PipeDataAjustadaPipe } from './pipe-data-ajustada.pipe';

describe('PipeDataAjustadaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDataAjustadaPipe();
    expect(pipe).toBeTruthy();
  });
});
